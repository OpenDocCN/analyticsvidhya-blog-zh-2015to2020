# 数据科学的 Python 基础— I

> 原文：<https://medium.com/analytics-vidhya/python-basics-for-data-science-i-7845ff1f3c6e?source=collection_archive---------13----------------------->

## #100daysofmlcode 挑战赛的第 1 天和第 2 天

![](img/caae64417ae76dc400e60e0d40a3da0d.png)

照片由 [Hitesh Choudhary](https://unsplash.com/@hiteshchoudhary?utm_source=medium&utm_medium=referral) 在 [Unsplash](https://unsplash.com?utm_source=medium&utm_medium=referral) 上拍摄

## 为什么数据科学家选择 Python 而不是其他语言？

从语法角度来看，Python 一直被认为是一种简单的编程语言。此外，它还有一个活跃的社区，有大量可供选择的图书馆和资源。

从事数据科学应用的专业人士不希望被复杂的编程需求所困扰。他们希望使用 Python 来轻松完成任务。

## 内容

*   变量
*   数据类型
*   目录
*   使用列表
*   元组
*   字典

Python 是一种解释的、面向对象的高级编程语言。Python 简单易学的语法强调可读性，因此降低了程序维护的成本。程序员将 python 用于不同的用途，比如:开发游戏、构建 web 应用程序、应用程序等等。

在继续学习这些 python 基础知识之前，我建议参考我之前的博客 [**为机器学习建立一个工作环境。**](/analytics-vidhya/setting-up-working-environment-for-machine-learning-a1b7affddc91)

# 变量

基本上，你可以说变量是一个包含值的单词。这意味着每个变量都包含一个值，每个值都是与该变量相关联的一条信息。例如，如果您想在变量中存储 10。

我们刚刚给变量 a 赋值。Python 支持不同的数据类型，如`int`、`float`、`string`和`boolean`。让我们看看 python 的不同数据类型，以及如何在其中声明和赋值。

# 数据类型

1.  **琴弦**

字符串就是一系列字符。在 python 中，单引号(`‘`)或双引号(`"`)内的任何内容都被视为字符串。

2.**整数**

整数用于存储整数。让我们来看看 python 是如何管理整数以及可以对整数执行的不同操作的。

3.**浮动**

Python 将任何带小数点的数字视为浮点数。在 python 中，我们可以执行与整数类似的操作。让我们来看看。

# 目录

列表是可以在任何时间点改变/修改的项目的集合。您可以创建一个包含字符串、浮点数、整数和布尔值的列表。在 python 中，方括号(`[]`)代表列表。让我们看看如何创建包含相同数据类型的列表。

如我所说，在列表中，我们可以存储多个数据类型值。看一看。

像大多数编程语言中的数组一样，列表索引从 0 开始，这意味着要访问第一个元素我们需要使用索引 0。除了 python 有特殊 index -1，它是最后一个元素的索引。看一看。

# 使用列表

1.  **制作号码列表**

在 python 中，我们可以使用`range()`函数创建一系列数字。让我们来看看如何使用`range()`功能创建列表。

2.**循环列表**

在数据分析中，您经常需要遍历列表中的每一项。让我们看一看。

3.**带 for 循环的 Range()函数**

我们也可以在 for 循环中使用`range()`函数——有时需要循环遍历一系列数字。让我们来看看代码。

4.**基本统计与列表**

您可以使用 min()、max()和 sum()等列表执行简单的统计。

## 预先列表操作

1.  **切片列表**

要对列表进行切片，必须传递起始索引和最后一个元素索引。像 range()函数一样，slice 也在一个元素之前停止。如果你想要元素从开始到第 5 个元素，你必须传递 0(第一个元素索引)到 5(第六个元素索引)。让我们来看看如何用代码实现它。

2.**遍历切片列表**

之前，我们已经看到了如何循环遍历列表以及如何对列表进行切片。甚至我们可以循环遍历切片列表。让我们看看如何做到这一点。

3.**复制列表**

在进行数据分析时，可能需要对现有列表和现有列表的副本进行操作。让我们看看如何复制列表。

# 元组

和列表一样，元组也是 python 中的集合类型。然而，元组是**不可变的**，这意味着不可变列表的**值不能改变**。在 python 中，元组使用括号“(”。让我们来看看如何在 python 中定义元组。

**基本元组操作**

1.  **修改元组**

我们知道，元组是不可变的列表类型，这意味着它不能被修改。但是，我们可以覆盖元组，这意味着我们可以完全改变元组值。

**2。遍历所有元组值**

在 python 中，我们可以像处理列表一样遍历元组。

与列表相比，元组是简单的数据结构。当程序员希望存储一些在整个程序中不能改变的值时，通常会用到它们。

# 字典

在 python 中，字典是键和值对的集合。在字典中，每个键都与值相关联。要访问任何值，我们必须使用与值相关联的键。我们可以在字典中使用任何数据对象作为键或值。让我们创建一个简单的字典。

**基本字典操作**

1.  **修改字典**

像列表数据收集方法一样，我们也可以修改字典。让我们来看看代码。

**2。从字典中删除键值**

**3。在字典中循环**

像列表和元组一样，我们可以使用循环来检查字典的每个键和值。

**4。嵌套字典**

在 python 中，可以在列表中存储字典，反之亦然。首先，让我们看看如何创建字典列表。

甚至你可以在字典里储存一本字典。

今天我们从 [**python 速成**](https://www.amazon.in/Python-Crash-Course-Eric-Matthes/dp/1593279280/ref=sr_1_3?crid=1ADH61109OK79&dchild=1&keywords=python+crash+course&qid=1608032075&sprefix=python+%2Caps%2C1246&sr=8-3) 教材中学习了 Python 的基础知识。我参加这个 100 天机器学习挑战的目标是从零开始学习机器学习，并帮助其他想开始机器学习之旅的人。许多概念对我来说是已知的，但我从零开始，帮助机器学习社区的初学者和修改概念。

感谢您的阅读。

要阅读这个话题的后续博客，请关注我的 [**Medium**](https://rishikonapure.medium.com/) **。**

要获得#100daysofmlcode 挑战赛的最新消息，请在 insta gram[](https://www.instagram.com/mlwithrish/)****上关注我。****

**希望你会喜欢我之前的文章— [**我为什么选择数据科学？**](/analytics-vidhya/why-i-chose-data-science-b98cee42ecbd)**